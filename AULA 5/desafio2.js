let valores = parseFloat(prompt("Digite a quantidade de produtos:"));

for (let i = 0; i < valores; i++) {
    let valor = parseFloat(prompt(`Digite o valor do produto ${i + 1}`));
    let soma = 0 + valor;
}
